child:
  ${file(./child.yml)}


childValue:
  ${file(./child.yml):key}


childBaked:
  ${file(./childVariable.yml), variable=value}

childJs:
  ${file(./child.js)}

childDir:
  ${file(./dir/child.yml)}

childRequire:
  key: ${require(chai):version}
