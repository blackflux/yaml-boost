child:
  ${file(./child.yml)}


childValue:
  ${file(./child.yml):key}


childBaked:
  ${file(./childVariable.yml), variable=value}

childJs:
  ${file(./child.js)}

childRel:
  ${file(./dir/child.yml)}
